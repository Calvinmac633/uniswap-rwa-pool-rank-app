import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // This app only ever reads public, permissionless RPC/API endpoints server-side
  // (no secrets), so no special runtime config is needed here.
};

export default nextConfig;
